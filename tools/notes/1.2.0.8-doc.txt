- **正式版 v1.2.0.8**（versionCode 250，com.xixi.hiking，**Release+R8 签名包**）—— 主工程 `hiking-app3/` 即正式版，改代码直接在这里
- v1.2.0.8 更新（**修 v1.2.0.7 引入的庆祝失灵 + 一览弹窗观感**，用户三反馈）：**① 庆祝卡与彩屑不弹（真 bug，上一版引入）**——根因：`saveRecord()` 内 `updateStatistics()` **先于** `checkMilestones()` 执行（1617 vs 1626 行），而 v1.2.0.7 把「静默补领」插进了 `updateStatistics()` 开头 → 新达成档位被静默路径**抢先标记为已完成**，随后本该弹庆祝的那次查不到新档 → 庆祝卡与彩屑永不出现（用户报「完成里程碑之后庆祝动画没了 / 庆祝彩带也没了」；实测确认彩屑本身正常：两条庆祝路径各 **400 粒 + animationName=confetti-fall**，关键帧完好）；**修法**：静默补领移出 `updateStatistics()`，改由 `app-init.js` 的**两个页面级时机**触发 —— ① 启动（`updateStatistics()` 之后）② `switchTab('overview')`，使其绝不与保存流程抢；**② 浅色模式一览文字看不清**——玻璃卡（8% 白）叠加深遮罩后**合成底色实测 #9A9EA9 中灰**，`#64748b` 描述色对比度仅 **2.07:1**（下限 4.5）；修法：遮罩统一为其他弹窗的 `rgba(0,0,0,0.35)`（深色 0.72）+ 浅色次要文字 `#64748b` → **`#1e293b`**，实测底色 #ABABAB、标题 **7.77:1** / 描述 **6.37:1**；**③ 弹窗尺寸与「记录查看」弹窗统一**——原为 `400px / width:100% / padding:16px / max-height:82vh`、遮罩 0.45，实测卡宽 358 且 82vh 顶满屏；改为与记录弹窗完全一致（`max-width:440px / width:calc(100vw - 44px) / padding:20px + iOS 安全区 / border-radius:20px / 遮罩 0.35·0.72`）→ 实测卡宽 **346（完全一致）**；行内边距 9→6px，`max-height` 收为 **70vh**（17 行内容实测 1154px 必然超出，86vh 会上下贴边；70vh 上下各留白 127px + 内部滚动）；**④ 工具**——`ghsync` 同步清单补入 `_test_p0p3.js`（262 断言）与 `_test_batch3.js`（原仅存本地，违背「本地不能是唯一副本」原则）；**⑤ 测试**——`_test_p0p3.js` 补检查断言触发方式改为 `checkMilestones(true)`，新增 **4 项回归**（统计不再抢先补领 / 庆祝卡如期弹出 / 关闭后落盘 / 彩屑 400 粒）→ **258 → 262**，全套自检 **6 套 540 项全绿**（smoke 19 / ios 10 / test 195 / test-ui 30 / P0P3 262 / E2E 24）
