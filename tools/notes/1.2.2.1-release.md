## v1.2.2.1 更新内容

【修复】
- **更新弹窗的更新内容排版修正** —— 「发现新版本」弹窗里的内容以前是纯文本输出（`white-space:pre-wrap`），`**小标题**` 和前面的短横线都会原样露出来，与设置页「更新日志」的观感不一致；现在两处走**同一套渲染器**：分组标题（【新增】【优化】…）加粗、每条带**序号 1. 2. 3.**（每组从 1 重新排）、小标题加粗、内容长照常滚动（弹窗 327px / 视口 200px 实测可滚）

【优化】
- **记录页说明行瘦身** —— 去掉「颜色说明」入口（山册顶部那行海拔图例已经够用），说明行恢复完整文案「山册视图 · 按山峰汇总成册，一座山一张卡片」

【内部】
- 清理：图例弹窗函数（约 3 KB）、`#ridgeLegendLink` 元素、5 组已无引用样式（`.view-caption-link` / `.ridge-legend` / `-row` / `-txt` / `-name`）、1 处事件绑定、9 条断言；`.ridge-legend-bar` 高度定义并入基础规则（15 → 11px）
- 更新日志 / 更新弹窗统一走 `renderChangelogBody()`；配色抽成公共函数 `changelogPalette()`（此前 `CL/MUTED` 是更新日志弹窗的局部量，更新弹窗用不上）
- 条目编号实现：`itemNo` 遇分组标题重置为 0，编号用 `min-width:15px + 右对齐 + tabular-nums`（两位数不跳动）
- 新增回归守卫 3 条：更新弹窗共用渲染器 / 共用配色函数 / 条目按数字编号且每组重置；修正 1 条因配色抽取而失效的旧断言（`var CL = {` → `function changelogPalette`）；`test.js` 219 → **226**
- 补测试盲区：`_test_p0p3.js`（jsdom）里 `showUpdateModal` / `renderChangelogBody` **均为 undefined** → 运行时断言会静默跳过（不打印、条数不变）；现加明确提示，校验改由 `test.js` 源码守卫承担

Made by XiXi 💛
