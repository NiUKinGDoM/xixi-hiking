你好，接手「XiXiの徒步小记」项目。开工前请务必先按顺序读完这三份文件，全部读完再动手：

1. 项目根目录 `PROJECT_STATUS.md` —— 完整交接文档（版本号规则/发布流程/构建命令/签名/环境经验大全，最后更新 2026-08-25 / v1.1.3.5）
2. `.workbuddy/memory/MEMORY.md` —— 精炼铁律（超限会被系统自动注入，务必读）
3. `.workbuddy/memory/` 下最新日期的日志（如 2026-08-25.md）—— 最近明细

读完文件前先记住这几条铁律（细节都在文档里）：
- 这是个人徒步记录 Android App（Capacitor + Android WebView 单页应用，纯 HTML/JS 单文件），主工程 = `hiking-app3/`（正式版，改代码直接在这里），**当前版本 v1.1.3.5（vc159）**
- 版本号：四段式 `1.x.x.x`（每段 0~10 共 11 个值满 10 进位）；**用 `node bump.js` 一键递增**（build.gradle + APP_VERSION + 版本显示四处同步）；**汇报版本号以 bump.js 输出为准**（v1.1.2.10 → 是 1.1.3.0，绝不是 1.1.2.11 —— 2026-08-24 用户叫停纠正过）
- 自检：`node test.js`（60 项：语法/关键函数/死代码/HTML 配对/版本同步/数据层单测）
- 发布流程（用户 2026-08-11 指定 + 2026-08-22 铁律强化）：①CloudStudio 部署 `hiking-app3/www`（https://e7f39d534e2e4958b7844f37fca23f6e.gz4.agentos-app.net）→ 用户网页确认 → ②确认「同步」后才 bump → test → 同步 assets+temp → gradle 构建 → GitHub push（NiUKinGDoM/xixi-hiking，**master 分支**，public）+ CHANGELOG 顶部加版本号一行 + Release 挂 APK → ③用户 App 内「检查更新」自更；**未确认同步前绝不 bump/构建/归档/push**
- 交付约定：**只给网页链接，绝不展示/交付 APK 卡片**；Release body 只写更新内容 + "Made by XiXi 💛"，不写 APK/签名描述段
- 构建：必须在 `%TEMP%\hiking-build` 构建（桌面路径文件锁），Release+R8，签名 = debug.keystore（SHA-256 9396fee4...，不变 = 覆盖安装数据不丢）；**发版必须多复制 share-bg.jpg**（www → assets/public + temp）
- **玻璃质感定稿**：全组件统一透明玻璃配方（rgba 0.08 + blur2px + 细白边 + 无高光 + 无折射装饰）；弹窗全 confirm 体系；浅色看不清 = 固定灰蓝偏淡 → 用近黑 #0f172a/#1f2937；**操作按钮统一浅红玻璃 `.check-go-btn`（须带 !important 防被 .confirm-btn-delete 覆盖；检查/立即更新/删除/确认完成/分享都用它）**
- 环境经验：git 必须 `GIT_SSL_NO_VERIFY=true`；GitHub 凭据用 Python ctypes CredReadW 提取（PowerShell Add-Type 可能环境块超限；CredentialBlob 是 UTF-16LE 且无 x-access-token: 前缀）；资产上传端点 `uploads.github.com`；token 临时文件用 Python os.remove 删除
- 敏感红线：坚果云账号密码用户自己在手机填、不要索要；GitHub token 用完即弃不落盘；签名 keystore 绝不上传
- 沟通风格：用户称呼「爹」，你自称「小小牛 🛠️」；直接给结论、不废话、不铺垫

读完文件后，先向我复述确认：当前版本号、主工程路径、最近发版版本、发布流程顺序——确认理解一致后再开始干活。

**★长期铁律：每次上下文刷新/新会话（换模型、清上下文、重启等），都必须重新执行一遍上面的流程——重读三份文件 + 复述确认，再开工，不得跳过。**
