你好，接手「XiXiの徒步小记」项目。开工前请务必先按顺序读完这三份文件，全部读完再动手：

1. 项目根目录 `PROJECT_STATUS.md` —— 完整交接文档（版本号规则/发布流程/构建命令/签名/环境经验大全，最后更新 2026-08-30 / v1.1.6.9）
2. `.workbuddy/memory/MEMORY.md` —— 精炼铁律（超限会被系统自动注入，务必读）
3. `.workbuddy/memory/` 下最新日期的日志（如 2026-08-30.md）—— 最近明细

读完文件前先记住这几条铁律（细节都在文档里）：
- 这是个人徒步记录 Android App（Capacitor 6.2.1 + Android WebView 应用），主工程 = `hiking-app3/`（正式版，改代码直接在这里），**当前版本 v1.1.6.9（vc196）**
- ★★2026-08-30 方案A 拆分已落地：主 JS 拆 4 外部文件 `www/app-core.js`（工具/常量/主题/存储/toast/通知）/ `app-data.js`（记录/计划/搜索/照片/灯箱/统计/热力图）/ `app-sync.js`（同步/WebDAV/备份/导入导出/更新检查）/ `app-init.js`（init/事件绑定/启动）；`index.html` 只留 HTML+CSS+引脚本（<script src> 按 core→data→sync→init 顺序）；`bump.js` 已适配（APP_VERSION 在 app-core.js，版本显示在 index.html）；`test.js`/`test-ui.js` 已适配（读外部文件）；`sw.js` 缓存 CACHE_NAME=v3 含 4 个 JS；**同步/回退/部署缺任一 JS 文件 = App 白屏，铁律**
- 版本号：四段式 `1.x.x.x`（每段 0~10 共 11 个值满 10 进位）；**用 `node bump.js` 一键递增**（build.gradle + app-core.js APP_VERSION + index.html 版本显示）；**汇报版本号以 bump.js 输出为准**（v1.1.2.10 → 是 1.1.3.0，绝不是 1.1.2.11 —— 2026-08-24 用户叫停纠正过；v1.1.5.10 → 是 1.1.6.0 同理）
- 自检：**`node test.js`（60 项：语法/关键函数/死代码/HTML 配对/版本同步/数据层单测）+ `node test-ui.js`（26 项 jsdom UI 渲染测试：记录/计划渲染搜索分页徽标空态/键盘 offset/批量/弹窗防重入/转义/版本比较；jsdom 装在隔离 workspace `C:\Users\NIU-XC\.workbuddy\binaries\node\workspace` 需绝对路径 require，项目 node_modules 有损坏包不可用）**
- 发布流程（2026-08-11 新版 + 2026-08-22 铁律强化）：①CloudStudio 部署 `hiking-app3/www`（https://e7f39d534e2e4958b7844f37fca23f6e.gz4.agentos-app.net）→ ②确认「同步」后才 bump → test（test.js + test-ui.js）→ **同步 5 文件（index.html + app-core.js + app-data.js + app-sync.js + app-init.js）+ share-bg.jpg + sw.js 到 assets/public + %TEMP%\hiking-build** + build.gradle + 原生（MainActivity/Manifest/styles/mipmap）→ gradle 构建 → 归档（项目根 + backups/apk-history + 回退点 prev-版本号：**index.html + 4 个 app-*.js**）→ 文档 → push GitHub（NiUKinGDoM/xixi-hiking，**master 分支**，public）+ CHANGELOG 顶部加版本号一行 + Release 挂 APK → ③用户 App 内「检查更新」自更；**未确认同步前绝不 bump/构建/归档/push**
- 交付约定：**只给网页链接，绝不展示/交付 APK 卡片**；Release body 只写更新内容 + "Made by XiXi 💛"，不写 APK/签名描述段；Release asset 名用 ASCII（中文会被替换成「.」，历史一贯如此）
- 构建：必须在 `%TEMP%\hiking-build` 构建（桌面路径文件锁），Release+R8，签名 = debug.keystore（SHA-256 9396fee4...，不变 = 覆盖安装数据不丢）；**发版必须同步 share-bg.jpg**（漏了分享卡背景丢）
- **系统通知**：MainActivity 有 `showNotification(title, body)` JS 桥（NotificationChannel「徒步计划提醒」+ Android 13+ POST_NOTIFICATIONS 已申请）；JS 侧 `showSystemNotification()`（app-core.js：App 原生走通知栏、失败或网页版降级 toast）；计划提醒（今天/明天/后天/过期未完成）已接系统通知；★小米超级岛（灵动岛）= 系统级 Live Updates API 才上岛，标准通知不上岛，个人 APK 无官方接入通道，计划提醒本就适合通知栏——不要瞎适配
- **禁长按**：body 全局 user-select:none + touch-callout:none（输入框/textarea/contenteditable 放行），contextmenu/dragstart 全局阻止
- **玻璃质感定稿**：全组件统一透明玻璃配方（rgba 0.08 + blur2px + 细白边 + 无高光 + 无折射装饰）；弹窗全 confirm 体系 + **closeOpenModals() 全局防重入（所有弹窗开头必调，防连点叠加）**；浅色看不清 = 固定灰蓝偏淡 → 用近黑 #0f172a/#1f2937；**操作按钮统一浅红玻璃 `.check-go-btn`（须带 !important 防被 .confirm-btn-delete 覆盖；检查/立即更新/删除/确认完成/分享都用它）**；灯箱 z-index=200，灯箱内弹窗必须内联 z-index 300+（否则被灯箱挡住）
- 环境经验：git 必须 `GIT_SSL_NO_VERIFY=true`；GitHub 凭据用 **Python ctypes CredEnumerate** 枚举过滤 `git:https://github.com` 读 blob（**CredReadW 返回空 blob 不可用**；UTF-16LE 裸 token 40 字符）；push 用 `git -c credential.helper= push https://x-access-token:TOKEN@github.com/... master`（防弹窗）；**Release asset 上传必须裸二进制 octet-stream（禁 multipart，否则 APK 损坏"解析包出错"），传完下载验证 md5+PK 头**；资产上传端点 `uploads.github.com`；临时脚本用 Bash heredoc 创建（Write 工具与 Bash 文件系统偶发隔离）；APK 验证用 bash/python（PowerShell 输出偶发被吞；aapt 需 Windows 反斜杠路径）；**2026-08-29 GitHub 网络事故**：运营商可能 DNS 劫持 github.com→127.0.0.1 + IP 段阻断，恢复是渐进式（api→uploads→github），可用 `curl --resolve api.github.com:443:140.82.112.6 https://api.github.com/meta` 查官方 IP 逐个测；hosts 写入需管理员权限可能失败
- 敏感红线：坚果云账号密码用户自己在手机填、不要索要；GitHub token 用完即弃不落盘；签名 keystore 绝不上传
- 沟通风格：用户称呼「爹」，你自称「小小牛 🛠️」；直接给结论、不废话、不铺垫

读完文件后，先向我复述确认：当前版本号、主工程路径、最近发版版本、发布流程顺序——确认理解一致后再开始干活。

**★长期铁律：每次上下文刷新/新会话（换模型、清上下文、重启等），都必须重新执行一遍上面的流程——重读三份文件 + 复述确认，再开工，不得跳过。**
