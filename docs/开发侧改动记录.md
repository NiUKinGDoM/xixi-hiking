# 开发侧改动记录（给爹看的）

> **为什么有这个文件** —— 你 2026-09-20 定的规矩：App 里的「更新日志」只写**你能感知到的内容**，不写开发侧（后端）做的事。
> 那些开发侧的活（代码清理、测试守卫、构建与性能实测、工具链修复、发布链路细节…）就记在这里，你想看明细时打开即可。
> **约定：每次发版追加一段、最新的在最上面**；技术细节的完整版同时在 `PROJECT_STATUS.md`「版本变更记录」里（给接手的人看）。
> 已接入双端同步（本地项目根 ↔ GitHub `docs/`），所以两边都看得到。

---

## v1.2.2.10（2026-09-23 · 你 v1.2.2.9 实测两条）

**App 内日志写了 3 条**（记录页删除与计划页统一 / 日历明细删除补边框 / 热力图分享按钮字色）。**下面这些没写进日志**：

- **① 记录页列表删除按钮与计划页列表统一**（你原话：「改的是计划页列表模式的删除按钮，不是文字，就那个灰色垃圾桶」）—— **★上一版 v1.2.2.9 我理解错了目标**：当时把「日历明细两处 `data-del`」和「记录页」一起动了，记录页还改成了**文字钮**。你纠正后正确定位：计划页列表删除 = `delete-planned-btn`（`confirm-btn-cancel` + `plannedDelStyle`）→ **灰色垃圾桶图标**。修法：记录页删回同款图标钮，`recordDelStyle` 与 `plannedDelStyle` 逐字一致。
- **② 计划页日历明细删除补边框**（你原话：「日历模式的这个删除按钮怎么没有边框」）—— 根因是我自己加的 `.danger-subtle-btn` 带 `border:transparent !important` 吃掉行内边框。修法：删 `.danger-subtle-btn`（已无引用），日历明细恢复 `confirm-btn-cancel` + 可见边框。
- **③ 热力图弹窗「分享」按钮字色**（你原话：「分享按钮的文字颜色不对」）—— 根因 `.hm-share-btn` 浅色写死 `color:#ffffff !important`；换 class 前 `.check-go-btn` 的 `color:#b91c1c !important` 恰压在其后 → 显示红字可读，换掉后只剩白字 → 浅色弹窗白字看不见。修法：浅色 `#334155`、深色 `#e2e8f0`。
- **守卫与反向验证**：`test.js` 312 条（危险名单移除行内删除 + 新增「行内删除＝中性灰+可见边框」「分享按钮字色随主题」）；**反向验证 3/3** 各自精确报红 → 逐字节还原。
- **★踩坑（新铁律已入记忆）**：① 切片锚点用 `;` 定界撞字符串字面量 → 残码（复现 v1.2.2.9 的坑）；② 拼转义字符串的 python 脚本反复对不上 → 最终改用 **Edit 工具精确替换**；③ 反向验证脚本锚点撞「记录页编辑弹窗取消按钮」同前缀。
- **自检**：`test` **312/0** · `test-ui` **30/0** · `P0P3` **299/0** · 弹窗宽度 **38/0** · iOS **10/0** = **689 项全绿**；审计残留物 **0/0/0**。⚠ `smoke`(20) / `e2e`(126) 仍未跑（EBUSY 未恢复）。
- **发布方式**：你选了「先只推网页版给我验」→ 我先 bump sw **v49→v50** + 镜像 push（commit `9fd4e43`）让你在浏览器验收；你「继续」后走完整发版：回退点 `prev-1.2.2.9/` → 版本升号 v1.2.2.10/vc274 → sw **v50→v51** → 构建 → 验包 14 项 → 镜像 push → Release + APK。
- **说明**：本版改**可见界面** → 升号。官网 `site/` 仍未部署（你「不急」）。

---

## v1.2.2.9（2026-09-23 当晚 · 你 v1.2.2.8 实测两条）

**App 内日志写了 3 条**（同意弹窗两按钮不再一大一小 / 同类另三个按钮一并修好 / 记录页删除按钮改文字）。**下面这些没写进日志**：

- **① 「使用前确认」弹窗两个按钮一大一小 —— 根因是我自己上一轮埋的**：弹窗底部按钮尺寸由一条 `.confirm-modal-buttons .confirm-btn-cancel, .confirm-btn-delete, .check-go-btn { padding:10px 24px !important; min-width:96px !important; … }` 规则统一，而 v1.2.2.7~08 那轮「红色只给危险操作」把「同意并继续」从 `check-go-btn` 换成了 `glass-btn` → **不再命中该规则** → 回落到 `.glass-btn` 自身内边距 → 与旁边「暂不同意」一大一小。**同类共 4 个按钮**：同意并继续 `legalAgree`、日期选择器「确定」`dtpOk`、保存二维码 `support-save`、分享 `hm-share`（**前两个是 v1.2.2.7 就留下的，当时没发现**）。修法：把 `.glass-btn`（含 `:disabled` —— `apOk` 的置灰态也一起丢了）纳入该规则。
- **② 记录页删除按钮改成与计划页同款**：记录页原为**图标钮**（`padding:6px` 方形 + `material-icons delete`），计划页是**文字钮**「删除」（`padding:6px 12px;font-size:12px`）→ `recordDelStyle` 改成与 `delStyle` **逐字一致** + 图标换文字「删除」。
- **★修 ② 时踩的坑**：用 `S.index(';', k)` 给样式块定界做切片 → **撞上字符串字面量里的 `;`**（`'padding:6px;…'`）提前截断 → 替换只落半行、**留两行残码 → 语法错** → 改用**行边界**精确定位后清除。**已入记忆：目标块含字符串字面量时不能用 `;` 当结束标记。**
- **守卫与反向验证**：`test.js` 310 → **312/0**（+2 条：弹窗尺寸统一 / 记录页删除＝文字）；**反向验证 2/2** 各自精确报红 1 条 → 逐字节还原后 312/0 ✓。
- **自检**：`test` **312/0** · `test-ui` **30/0** · `P0P3` **299/0** · 弹窗宽度 **38/0** · iOS 适配 **10/0** = **689 项全绿**；审计残留物 **0/0/0** · 外部依赖 **0**。⚠ **`smoke`(20) 与 `e2e`(126) 仍未跑**（EBUSY 故障未恢复 → 视觉基线本版未刷新，恢复后补跑）。
- **★你新定规矩**：**Release 正文＝App 更新弹窗显示的内容**（`info.body`）→ `tools/notes/<版本>-release.md` **只放更新日志本身**，不放「📱 安装 / 🌐 网页版」那两行。已改两个模板 + 用 API `PATCH` 把两个已发布 Release 的正文修掉。
- **发布链路**：回退点 `backups/prev-1.2.2.8/` → sw 缓存 **v48 → v49** → 构建 **2.40 MB**（R8）→ 验包 **14 项全过** → 镜像 push → Release + APK 上传（服务端 digest 校验 + 下载回验）→ 网页版复核。
- **说明**：本版**改了可见界面**（弹窗按钮尺寸 / 记录页删除按钮）→ 按常规**升号**（你在对话里选了「升号 v1.2.2.9」）。官网 `site/` 仍未部署（你说「不急」）。

---

## v1.2.2.8（2026-09-23 · 你定「按你想的弄」）

**App 内日志写了 3 条**（完成类按钮去红 / 行内删除改淡红图标 / 政策补邮箱）。**下面这些没写进日志**：

- **行内删除改「淡红图标」**（你问「29 行全是红删除钮，你想怎么改？」→ 你回「按你想的弄」→ 取方案 A）：新增 `.danger-subtle-btn`（`background/border transparent !important`、浅色 `#b91c1c` / 深色 `#fca5a5`、hover/active 才给极淡红底），3 处（记录列表 / 计划列表 / 日历明细）从 `.check-go-btn` 换过来。**★根因**：这 3 处的内联灰蓝样式**一直是死代码** —— `.check-go-btn` 带 `!important`，而 **CSS `!important` 优先级高于内联样式**，所以内联写的 `background:rgba(100,116,139,0.14)` 从来没生效过，一直显示成红框按钮。
- **「完成 / 编辑 / 保存 / 确定 / 分享」等 17 处非危险按钮去红**：v1.2.2.7 只收了 9 处，本轮扫全量后补齐 17 处（`dfOk` / `mwpOk` / `legalAgree` / `support-save` / `hmyp-ok` / `hm-share` / `完成·延期`×2 / `apOk` / `oc-go` / `pd-edit-btn` / `pd-complete-btn` / `save-planned-btn` / `confirm-complete-ok` / `celebrateOkBtn`×3）→ 全部 `.glass-btn`。**顺带发现**：「确认完成」还挂着 `.confirm-btn-delete`（**实色红** `rgba(239,68,68,.85)`）—— 只换按钮 class 不够，那个类也得摘掉。
- **政策补邮箱 + 版本 bump**：第十节加 `mailto:Xixihiking@foxmail.com`；生效日期 / 最近更新 → **2026-09-23**；`LEGAL_VERSION` → `'2026-09-23'`。**★代价已提前告诉你**：政策正文变更 → 所有用户下次打开会**重新确认一次条款**。另新增 `.dmi-body a` 样式（此前无规则 → 浏览器默认蓝，深色下几乎不可读）。
- **★自查揪出 4 个测试文件把条款版本写死**（改完 LEGAL_VERSION 后 `test-ui` 冒出 4 条失败：「管理弹窗 / 下载弹窗连开两次仅 1 个」count=2 —— 预置的「已同意」版本号是旧值 → 同意弹窗（带 `data-persist`、`closeOpenModals()` 豁免）**残留**把弹窗计数顶多 1）。修法**根治**：`test.js` 守卫内联 `dj.match(/const LEGAL_VERSION = '([^']+)'/)` 与该值强绑定（**既防忘 bump，也防 bump 了没改正文**）；`_test_p0p3.js` / `test-ui.js` 由 `allJs` 提取；`e2e/run.js` 由源码提取后经 `addInitScript(fn, LEGAL_V)` 传入。顺带把 `test.js` 里那条**写死 `生效日期：2026-09-18`** 的旧守卫一起改掉。
- **★环境故障（本版全程绕行）**：本机 **node 启动任何子进程一律 `EBUSY`**（`spawnSync` 连 spawn `process.execPath` 都是 EBUSY；shell 里 git / python / node 自身都正常）→ **所有「包装型」工具失效**：`ship prepare/publish`、`checkall`、`smoke`、`patch.js`（写后 `node --check`）、`ghsync`（spawn git 取 token）、`ghrelease`（spawn ghtoken）、`release.js`（spawn gradle）、`verify-apk`（spawn aapt/apksigner）。**绕行**：① 注入改「python 唯一命中校验 + 落盘 + shell 侧 `node --check` + 失败还原」等价流程；② 自检按 `checkall` 的 SUITES 清单**逐个直跑**；③ `prev-snapshot` / `bump` / `security obf|hash` 直跑，`builtin`（注入 + 禁用词门禁）/ `docrelease` / `ghsync` / `ghrelease` / `verify-apk` 用 python 等价复刻；④ 构建直接 `java -classpath gradle-launcher … GradleMain assembleRelease`；⑤ push 手拼 git 命令（token 只进变量 + 打码）。**⚠ 遗留**：`smoke`(20) 与 `e2e`(126) 本版**未能执行**，等 EBUSY 恢复需补跑 + 刷视觉基线。
- **自检**：`test` **310/0**（309 + 1 条新守卫「行内删除＝淡红图标」）· `test-ui` **30/0** · `P0P3` **299/0** · 弹窗宽度 **38/0** · iOS 适配 **10/0** —— 共 **687 项全绿**（`smoke` 20 + `e2e` 126 因上述故障未跑，**明确标注未伪造**）。
- **官网 `site/` 设计语言对齐 App + 视觉打磨**（你说「网页再改改再上线，不急。整体的设计语言改一下，和 app 一样，你再优化一下」）：重写 `site/assets/site.css` —— 玻璃配方**全站唯一的 `blur(2px) saturate(150%)`**（此前混用 10/12/14px，违背 App 铁律）、卡片玻璃底 `0.72 → 0.6`、色板对齐（标题 `#0f172a` / 正文 `#334155` / 次要 `#52606f`）、圆角表补齐（卡 20 / 子 16 / 钮 12 / 输 10 / 滚 4）、滚动条细圆角 4、`focus-visible` 焦点环、`prefers-reduced-motion` 下关平滑滚动；段落标题改**「图标 + 文字」内联写法**（对齐 App 的 `h2 > icon + title-text`）+ 信息型 `.eyebrow`；三页生效日期同步、更新日志段补 v1.2.2.8。**本轮不部署**（你明确「不急」），代码已推仓库。
- **发布链路**：回退点 `backups/prev-1.2.2.7/` → sw 缓存 **v47 → v48** → 构建 **2.40 MB**（R8 跑过，3m04s / 77 tasks）→ 验包 **14 项全过**（包名 `com.xixi.hiking` vc272 / 签名白名单 `9396fee4…` / JS 已混淆 / ResGuard **9 项与 APK 内容全匹配**）→ 镜像 push `a81b1f8..7c13445`（16 files, +380/−91）→ Release **新建 id=394489187** / asset **id=583421882 / 2512364 B**，服务端 digest(sha256) 与本地**逐位一致** + 下载回验（PK 头）通过 → 网页版复核 **sw v48 + `danger-subtle-btn`（CSS 5 处 / 模板 3 处）+ `APP_VERSION 1.2.2.8` + BUILTIN 含 v1.2.2.8 + 邮箱 + `LEGAL_VERSION 2026-09-23`** ✓
- **小插曲**：`curl` 在本沙箱对 `api.github.com` / `github.com` 一律 000（含 `--noproxy`），但 **python urllib 直连 HTTP 200** → Release 创建与上传改用 python 完成；`ghrelease.js` 那套「钉 IP 绕 hosts 劫持」本机这次不需要。

---

## 官网 site（2026-09-23 · 新增，非 App 发版）

**App 内更新日志不会提这件事**（官网不属于 App 功能），但它是本次交付的一部分：

- **新增 `hiking-app3/site/`**：首页（首屏 / 它是什么 / 界面 / 功能 / 隐私与数据 / 更新日志 / 页脚）+ 隐私政策页 + 免责声明页，纯静态、零外部 CDN、零构建
- **为什么单独一个目录**：`www/` 是 App 本体（PWA），`sw.js` 会接管作用域 → 官网放进去会「打开官网直接进 App」。所以用**独立 CF Pages 项目**（Root=`site`）
- **政策正文与 App 同口径**：10 节隐私政策 + 10 节免责声明逐条转录自 `www/app-data.js` → **两份拷贝，改一处必须改另一处**（已写进 `site/README.md` 的「两个易踩的坑」）
- **界面截图是真实 App 渲染**（非手绘）：起本地服务 → 调 `loadSampleData()` + 补 26 条演示记录 → 截图（统计概览 / 徒步足迹 / 山册 各 390×844 @1.5x）。顺带发现两处不适合当宣传图的真实状态：① 记录列表 29 行全是红删除钮（语义正确但视觉很吵）② 计划列表「完成」按钮也是红（**同属红色语义误用，本次未改，待你定**）
- **自检**：playwright 实测 —— **0 个外部请求**、0 请求失败、站内链接全 200、320/390/768/1280 四档**零横溢**；对比度：正文 6.75:1、链接 5.6:1、次要文字 4.83:1（原 #64748b 只有 4.24:1，已提深）
- **接入同步链**：`tools/ghsync.js` 的 DIRS 加 `site` 全量同步 + diff 核对加 site 三页（15 项）；已 push（`879f677..5d3f092`）
- **★首次部署待你操作**：CF 控制台 → Workers & Pages → Create → Pages → Connect to Git → 选本仓库 → Build command 留空 / **Build output directory = `site`**（步骤写在 `site/README.md`）
## v1.2.2.7（2026-09-23 · 你实测反馈三条 UI 问题）

**App 内日志写了 4 条**（删除才显红 / 小屏标题不再竖排 / 时间显示人话 / 输入框玻璃化）。**下面这些没写进日志**：

- **红色语义收口**：`.check-go-btn`（危险红 `#b91c1c`）此前被 **9 处非危险操作**误用（保存 / 编辑这条记录 / 绑定账号 / 支持作者 / 照片弹窗「优化」/ 选择器「确定」/ 4 个引导按钮）→ 全部改 `.glass-btn`；反向补上 **3 处行内删除**（记录列表 / 计划列表 / 日历明细）由中性灰改红 —— 此前「主操作与危险操作长同一张脸」
- **标题防挤体系**：`.title-text{white-space:nowrap}` 只解决「折行」，**会把「竖排」换成「整行横溢」**（实测 360px 内容 355 > 可用 322，页面可横向拖动）→ 新增 `.panel-hdr`（`flex-wrap:wrap` + 控件组 `margin-left:auto` 右对齐兜底 + `row-gap:8px`）+ `@media(max-width:430px)` 收紧内边距/间距/徽标字号；顺带修掉 CSS 里**写错的 id**（`#plansTitle`/`#overviewTitle` → 真值 `#plannedTitle`/`#statsTitle`/`#heatmapTitle`，即计划页/统计页此前根本没被这层保护罩住）
- **时间字段「显示层 / 存储层」分离**：显示改 `YYYY-MM-DD HH:mm`，新增 `data-iso` 携带 ISO 真值（picker 写回 + 保存优先读 `dataset.iso`）→ 保证「好看」不影响「存对」
- **两处自测发现的连带问题**：① 「时 / 分 · 里程」复合框（`.edit-merge-box`）的白底藏在 **app-data.js 模板的内联样式**里、且写法**逗号后无空格** → 上一轮守卫写死带空格写法，**假阴性漏判**，实际一直没玻璃化；② 玻璃化把浅色占位符对比度从 5.1:1 拉到 **3.6:1**（本次引入的回归，必须回补）
- **对比度像素实测（非源码断言）**：浅色 4.91~6.60:1、深色 4.86~14.46:1，**全字段 ≥ AA**；标题在 320/360/390/412 四档实测（恒横排、零横溢）
- **测试守卫**：`test.js` 287 → **308**（+21 条，含修掉一条漏判的）；**反向验证 6/6** 各自精确报红 1 条 → 逐字节还原后 308/0；全量自检 810 → **831 项全绿**；代码审计残留物 **0/0/0**；设计体检符合规范表（圆角/字体/玻璃配方/层级均未动）
- **本轮新增铁律（已入项目记忆）**：① 断言别写死「值的格式」（空格差异 → **假阴性**），且查残留**必须覆盖 JS 模板里的内联样式**；② 改输入框底色（玻璃化）后必须复测「占位符 + 值」对比度，内联白底容器要**一起**玻璃化；③ 多行锚点必须自带唯一标识（否则同结构页面互相命中，报「命中 N 次」先查锚点）；④ `e2e/inspect.js` 视口写死 390，窄屏验收用「一宽度一进程」探针（**同进程反复 `setViewportSize` 会卡死**）；⑤ 脚本必须幂等（Bash 命令偶发被执行两次）
- **发布链路**：回退点 `backups/prev-1.2.2.7/` → sw 缓存 **v46 → v47** → 构建 **2.40 MB** → APK 验证 **14 项全过**
- **发布详情**：镜像 push `d7f35e4..1b95cff`（10 files, +886/−757）；Release **新建 id=394339488** → asset **id=583119116 / 2512426 B**，服务端 digest(sha256) 与本地一致；网页版复核 **sw v47 + `panel-hdr`×9 + 玻璃 0.42 + `#cbd5e1` + `data-iso`×6 + `puOpt` 已中性** ✓
- **小插曲**：`api.github.com` / `uploads.github.com` 又被 hosts 劫持到 `127.0.0.1`（Steam++），工具**自动钉真实 IP 绕过**，未影响发布；`pages.dev` 自动部署校验在 2 分钟窗口内超时（事后手动复核已上新）

---

## v1.2.2.6 · 修正重发（2026-09-20 傍晚）

**触发**：你定了新规矩 ——「更新日志只纯净写内容，不用体现 app 后端弄了些啥」「开发侧的你告诉我就行，要我能看得着的」。
于是我做了两件事：**① 把这条规矩做成工具硬拦**（以后想写也写不进去）；**② 把 App 内更新日志里历史上攒下的开发侧内容全部清掉，并同号重发一次**。

- **清理范围**：App 内日志里 **13 段【内部】分组**（从 v1.2.1.3 一直到 v1.2.2.6），共删掉 **3603 字符**；日志区从 26877 → **23274 字符**。清理后**整个日志只剩用户能感知的内容**
- **规则落地（工具层）**：`tools/builtin.js` 注入前两道硬拦 —— 含 `【内部】` 或含开发侧词（全量自检 / P0P3 / E2E / 守卫 / 断言 / 重构 / 死代码 / 视觉基线 / 性能实测 / ResGuard…）→ **直接拒绝注入**。做了反向验证：含【内部】拦 ✓、含「全量自检」拦 ✓、干净文案放行 ✓
- **本文件诞生**：开发侧内容从此写这里（双端同步：本地项目根 ↔ GitHub `docs/`）；`tools/doc-sync.js` 与 `tools/docaudit.js` 的配对表各加一行 → **双端文档从 7 组变 8 组**
- **同号重发（v1.2.2.6 不变）**：回退点 `backups/prev-1.2.2.6/`（11 文件）→ sw 缓存 **v45 → v46** → 全套自检 **810 项全绿** → 审计残留 **0/0/0** → 构建 **2.39 MB** → APK 验证 **14 项全过**
- **发布详情**：镜像 push `6a204a2..559a02f`（3 files, +18/−18）；Release **复用 id=392318699**（同号）→ 说明重写（928 字符，已无开发侧内容）→ 删旧 asset `576159596` → 新 asset `576525254` / **2510582 B**，服务端 digest 一致；网页版复核 **sw v46 + 日志区【内部】=0** ✓

---

## v1.2.2.6（2026-09-20 · 彻查全修）

**App 内日志只写了 5 条修复**（记录数值被填错、保存失败没提示、数据读取异常、导入损坏备份、抹掉足迹不彻底）。**下面这些没写进日志**：

- **代码清理**：记录字段的统一净化层补上距离与用时（此前只净化难度和海拔 → 负数距离能一路进存储）；删掉一个全项目没人引用的死属性 `data-diff`；`todayKey` 去掉一次多余的日期字符串往返
- **测试守卫**：新增 15 条守卫锁住这批修复（`test.js` 272 → **287**），每条都做了「撤掉修复必须报红」的反向验证；全量自检 795 → **810 项全绿**；代码审计残留物 **0/0/0**
- **浏览器实测复验**：距离 `-5`→0、`1e9`→10000；用时 `9999h`→1380 分钟；净化层负数→`0/0`、超大→`10000/1440`；损坏存储 → `getItem` 返回 null + 隔离键生成；`setItem` 返回 true/false；畸形 zip → toast 弹出；抹除 → 5 个 key 全清
- **踩到的两个工具坑（已入铁律）**：① 补丁的 `old` 含块收尾 `}` 时 `new` 必须把它带回来，漏了就是语法错（工具会拦下并还原）；② 连续单行断言漏 `;` 会被下一行开头的 `(` 连成调用链 → **断言静默不执行**（fail 仍是 0），靠「通过数应正好 +N」才揪出来
- **发布链路**：构建 **2.40 MB**（Release+R8 签名包）、APK 验证 **14 项全过**；镜像 push `b726d54..b4bb60b`（11 files, +125/−29）；Release asset **2514601 B**，服务端 digest sha256 与本地一致；网页版 `sw` 缓存 v44 → **v45**
- **发布时的小插曲**：`api.github.com` / `uploads.github.com` 又被 hosts 劫持到 127.0.0.1（Steam++），工具**自动钉真实 IP 绕过**，没影响发布

## v1.2.2.5（2026-09-20 · 五项优化）

**App 内日志写了**【新增】列表视图也能「完成/延期」+【优化】深色弹窗图标对比度。**下面这些没写**：

- **代码清理**：计划日差算法原先在 **4 处各写一遍**（到期判定 / 状态徽章 / 过期关怀 / 计划提醒）→ 抽成公共函数 `planDayDiff()`；改动前后跑探针比对，**行为逐字节一致**
- **性能实测**：概览/记录/计划/设置四个页签 **60.1~60.3fps**、p95 16.8ms、**卡顿帧 0**、无长任务；滚动 60.1fps；清理后列表每条计划渲染少构造 4 个日期对象
- **设计体检**：把「勾选框 5px 圆角」登记进规范表（此前全项目唯一一处未登记取值），体检恢复全绿
- **视觉回归**：概览页截图容差 0.5% → **1%**（玻璃卡密集且带入场动画，动画终态有 0.1%~0.6% 亚像素抖动；其余 9 张仍严格 0.5%）
- **测试守卫**：`test.js` 271、`P0P3` 299、`E2E` 126，全量 **794 项**；修 3 处注释错字（「靶蓝」→「靛蓝」）
- **新工具**：`tools/versiondiff.js`（列出「已发布版 vs 当前工作区」的全部改动，写日志前逐行核对，防漏写）
- **同号修正重发**：sw 缓存 v43 → v44，Release 同号覆盖（复用 Release id + 删旧 asset 重传 + PATCH 说明）

> 更早版本（v1.2.2.0 ~ v1.2.2.4）的开发侧内容，目前仍在 App 内更新日志的历史条目里（那时还没定这个规矩，之后的新版本会一律写到这里）。
