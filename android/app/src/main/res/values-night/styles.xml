<?xml version="1.0" encoding="utf-8"?>
<!-- ★2026-08-27 渐进增强：深色模式（跟随系统）下的主题覆盖：
     - 启动屏背景 = App 深色背景 #0a1220（不再闪白）
     - 底部导航栏深色 + 浅色图标（与 App 深色 UI 一致） -->
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">#0a1220</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- ★2026-08-27 防闪白：深色模式 window 背景 = App 深色背景（不再闪白） -->
        <item name="android:windowBackground">#0a1220</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0a1220</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="windowSplashScreenIconBackgroundColor">#0a1220</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
